# docker-centos8-base

This Docker image takes the official image from the CentOS project and modifies the time zone.
As such, this image is not for common usage, but I (vindevoy) will use it as my base image for all CentOS 8 images.

## Usage

```
docker pull vindevoy/centos8-base
```
